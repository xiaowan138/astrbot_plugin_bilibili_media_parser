# 更新记录

## v1.2.0

### 新功能

- 新增 B 站番剧解析：支持 `bilibili.com/bangumi/play/ep...`、`ss...` 链接和番剧短链，展示番剧标题、本集标题、地区、集数、进度、简介与追番/播放等统计。
- 新增开播提醒：开启 `enable_live_monitor` 后，使用“开播提醒 订阅 房间号”订阅直播间，主播开播时自动通知所在会话；支持订阅列表、单个取消、一键取消全部，订阅保存在本地，重启不丢失。重启后已在播的直播间不会立刻触发提醒，避免刷屏。
- 新增直播查询命令：发送“直播查询 房间号或直播间链接”可随时生成当前直播状态卡片，无需等待有人分享链接。
- 新增音频下载：开启 `enable_audio_download` 后，解析音频卡片会提示编号，发送“音频下载 编号”即可获取音频文件；直链来自 B 站音频接口，不需要妖狐 API Key，与视频下载共用冷却、每日上限、队列等全部限制。
- 新增多链接解析：一条消息里同时出现多个 B 站链接时按出现顺序逐个解析（数量由 `max_links_per_message` 控制，默认 3，上限 5）。
- AI 总结扩展到专栏和动态：开启 `enable_ai_summary` 后，专栏卡片会用专栏正文、动态卡片会用动态正文生成 AI 概要。

### 问题修复

- 修复用户空间主页（`space.bilibili.com/{mid}`）被误当成内容链接解析并报错的问题；现在空间主页链接会被忽略，带视频编号的空间链接仍正常解析。
- 修复长图缓存淘汰后图片文件残留的问题：缓存过期或被挤出的卡片图片现在会同步删除临时文件。

## v1.1.1

- 修复直播/专栏/动态分享无法解析的核心问题：`b23.tv` 短链或 QQ 小程序卡片重定向到直播间/专栏/动态后，解析器只接受视频编号，导致报错“没有从链接中找到有效的 BV/AV/AU 号”，甚至可能把直播页推荐位的视频误当成分享目标。现在短链会先解析出真实内容类型再分流。
- 修复 `live.bilibili.com/h5/...`、`live.bilibili.com/blanc/...` 直播链接无法识别的问题。
- 修复 `bilibili://live/...`、`bilibili://opus/...`、`bilibili://article/...` 小程序 URI 无法识别的问题。
- 直播卡片新增主播头像（此前直播卡片的头像位置永远是占位图）。
- 修复开启语音转写后，即使视频已有公开字幕也会下载完整视频转写的性能问题；现在转写仅在无字幕时作为回退。
- 语音转写不再强制按中文识别，改为自动检测语言，外语视频不会再被硬转成乱码。

## v1.1.0

- 新增 B 站专栏（CV）解析：支持 `bilibili.com/read/cv...` 链接，展示标题、作者、分类、字数、摘要与统计。
- 新增 B 站直播解析：支持 `live.bilibili.com/...` 链接，展示标题、主播、分区、在线人数与直播状态。
- 新增 B 站动态解析：支持 `bilibili.com/opus/...`、`t.bilibili.com/...` 链接，展示作者、内容、图片与点赞/评论/转发统计。
- 新增通用内容卡片模板（`templates/content_card.html`），统一渲染专栏/直播/动态。
- 插件名称更新为「B站全内容解析」。

## v1.0.1

- 修复 B 站音频解析接口错误：改用正确的 `www.bilibili.com/audio/music-service-c/web/song/info` 接口，并修正 UP 主 UID 字段读取（`uid` 而非 `mid`），音频解析现在可以正常工作。
- 短链解析支持识别音频（AU）链接。

## v1.0.0

- 首个版本。自动识别并解析 B 站视频/音频链接、`BV`/`AV` 号、`b23.tv` 短链和 QQ 小程序分享。
- 视频解析支持长图卡片：封面、UP 主、统计、热门评论与源链接。
- 音频解析支持 `bilibili.com/audio/au...` 链接的长图卡片与文本模式。
- 可选使用 AstrBot 已配置的模型商生成视频 AI 总结（基于公开字幕或登录后 AI 字幕）。
- 可选视频下载（基于妖狐直链），带编号有效期、权限、冷却、每日上限、并发与体积/时长限制。
- 可选 `faster-whisper` 语音转写总结（需手动安装 ffmpeg 与 faster-whisper）。
