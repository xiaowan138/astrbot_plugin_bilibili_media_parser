# 更新记录

## v1.4.0

### 问题修复

- **修复动态永远只能发出纯文本的问题**：动态内容模型缺少 `cover_url` 字段，通用内容卡片渲染流程读取它会抛出 `AttributeError`，被上层的“渲染失败降级”分支吞掉。结果所有 `bilibili.com/opus/...` 和 `t.bilibili.com/...` 链接都退化成文本消息，v1.3.0 的动态图集功能实际上从未生效，后台还会持续记录“长图卡片渲染失败”。现在动态模型带上封面字段，封面使用图集第一张图，图集只展示其余图片，不再重复。
- **修复 Python 3.10 环境下载排队会直接报错**：排队等待只捕获了内置 `TimeoutError`，而 3.11 之前 `asyncio.wait_for` 抛出的 `asyncio.TimeoutError` 并不是它的子类，超时异常会直接冒出下载流程、任务永久停留在“排队中”。现在两种类型都捕获。
- **修复下载命令被首尾空格打断**：手机上多打一个尾部空格，“视频下载 123456”就会静默无响应，而卡片提示语恰恰要求用户发送这句话。现在命令匹配前统一去除首尾空白（与直播命令保持一致）。
- **修复开播提醒要等群里有新消息才开始工作**：轮询任务只由消息入口引导启动，插件重启后如果长时间没人发言，已有订阅不会触发任何提醒。现在插件加载时即尝试启动，事件循环尚未就绪时仍回落到消息入口。
- **修复重载插件后残留渲染图片**：`terminate()` 只清空卡片缓存字典、没有删除对应的长图文件，多次重载会在渲染目录里堆积 JPEG。现在退出前一并删除，并清空其余内存缓存。
- **修复音频卡片头像始终为占位图**：B 站音频接口不返回上传者头像，现在与直播一样按 mid 补取一次用户名片，卡片头像正常显示。
- **清理**：移除未使用的导入；多条链接被 `max_links_per_message` 截断时不再静默丢弃，会明确提示“共 N 个、只解析前 M 个、剩余请分开发送”。

### 新功能

- **新增 UP 主空间卡片**：`space.bilibili.com/{uid}` 现在会解析成用户名片长图，展示昵称、头像、认证、等级、粉丝、关注、投稿数和获赞数以及个人签名。此前这类链接会被静默忽略（v1.2.0 的有意行为，本次改为主动解析）；带 BV 号的空间链接、以及合集/相册等没有稳定含义的空间子页仍按原样处理。
- **视频卡片新增分 P 列表**：多 P 视频在简介下方列出各 P 的序号、标题和时长，最多 10 P，其余只显示剩余数量；单 P 视频不显示该区块。
- **AI 概要缓存**：新增 `summary_cache_seconds`（默认 86400 秒）。同一视频在有效期内再次解析时直接复用已生成的概要，不再抓取字幕、也不再调用模型，避免热门链接反复消耗额度。
- **新增手动总结命令**：开启 `enable_manual_summary` 后，发送“视频总结 BV号或链接”可只总结这一个视频，不必为群里所有链接开启 `enable_ai_summary`；命令关键词可由 `manual_summary_keyword` 修改，未开启时该命令完全不拦截消息。
- **开播提醒改发直播长图**：开播通知现在附带完整的直播间卡片（封面、主播、分区、在线人数、二维码），取数据或渲染失败时自动退回原来的纯文本提醒；下播提醒保持纯文本。

### 测试

- **补上 main.py 的运行时测试**：此前 `tests/test_main_contract.py` 只对 main.py 的源码字符串做断言，无法暴露任何运行时错误——上面那个动态封面崩溃正是在 155 个用例全绿的情况下发布的。新增 `tests/astrbot_stub.py`（最小 astrbot 替身，可按相对导入加载 main.py）和 `tests/test_main_runtime.py`（17 个用例），真实执行卡片渲染流程、套用真实 Jinja 模板并断言产出的 HTML，覆盖：全部内容类型渲染、动态图集不重复、UP 主卡片不出现封面占位、分 P 列表、概要缓存命中、手动总结、消息路由与空格容错、下载排队取消。回退动态封面修复后，其中 2 个用例即失败，可确认这层测试确实能拦住该类回归。
- 全套测试从 155 个用例增加到 173 个用例（另含 7 个子测试），`ruff check` 无告警。

## v1.3.0

### 新功能

- 新增下播提醒：开启 `enable_live_end_notify` 后，已订阅的直播间在主播下播（或转为轮播）时也会通知所在会话；与开播提醒共用订阅列表和轮询任务，只开启其中一个即可订阅。
- 动态卡片支持多图展示：多图动态除封面外最多再展示 8 张图片（以图集形式渲染在卡片中）。
- 番剧卡片新增剧集列表：展示最近 8 集的集数与标题。
- 支持 `bilibili://audio/...` 小程序分享 URI 识别为音频。
- 发送“直播查询”但未带房间号时，会回复用法提示而不是静默忽略。

### 问题修复

- 修复“音频下载 编号”命令必然报错的问题：音频命令的正则只有一个捕获组，旧实现读取第二个捕获组会抛出 IndexError，导致音频下载完全不可用。
- 修复直播查询返回过期数据的问题：直播信息与直播卡片改用独立的短缓存 `live_cache_seconds`（默认 60 秒），不再沿用 300 秒的视频缓存。
- 修复“视频下载取消”（无空格）不被识别的问题；“视频下载 状态/查看/查询”的无空格写法也一并支持。
- 修复下载校验失败（分 P 不存在、超过时长上限、未配置妖狐 Key 等）后编号直接作废的问题：现在校验失败会返还编号，修正命令后可直接重试，无需重新发送链接。
- 修复开播/下播提醒开关在后台关闭后轮询任务仍然持续运行的问题：现在轮询循环会自动停止。
- 修复“取消全部”订阅后遗留直播间状态缓存的问题；单个取消最后一个订阅者时同样会清理。
- 直播状态轮询增加并发上限（5），订阅较多时避免对 B 站接口造成瞬时压力。

### 重构

- 下载/直播命令解析抽取到独立的纯函数模块 `bilibili_parser/commands.py`，并补充行为级单元测试（新增 `tests/test_commands.py`，全套测试共 155 个用例）。

## v1.2.0

### 新功能

- 新增 B 站番剧解析：支持 `bilibili.com/bangumi/play/ep...`、`ss...` 链接和番剧短链，展示番剧标题、本集标题、地区、集数、进度、简介与追番/播放等统计。
- 新增开播提醒：开启 `enable_live_monitor` 后，使用“开播提醒 订阅 房间号”订阅直播间，主播开播时自动通知所在会话；支持订阅列表、单个取消、一键取消全部，订阅保存在本地，重启不丢失。重启后已在播的直播间不会立刻触发提醒，避免刷屏。
- 新增直播查询命令：发送“直播查询 房间号或直播间链接”可随时生成当前直播状态卡片，无需等待有人分享链接。
- 新增音频下载：开启 `enable_audio_download` 后，解析音频卡片会提示编号，发送“音频下载 编号”即可获取音频文件；直链来自 B 站音频接口，不需要妖狐 API Key，与视频下载共用冷却、每日上限、队列等全部限制。
- 新增多链接解析：一条消息里同时出现多个 B 站链接时按出现顺序逐个解析（数量由 `max_links_per_message` 控制，默认 3，上限 5）。
- AI 总结扩展到专栏和动态：开启 `enable_ai_summary` 后，专栏卡片会用专栏正文、动态卡片会用动态正文生成 AI 概要。

### 问题修复

- 修复用户空间主页（`space.bilibili.com/{mid}`）被误当成内容链接解析并报错的问题；现在空间主页链接会被忽略，带视频编号的空间链接仍正常解析。
- 修复长图缓存淘汰后图片文件残留的问题：缓存过期或被挤出的卡片图片现在会同步删除临时文件。

## v1.1.1

- 修复直播/专栏/动态分享无法解析的核心问题：`b23.tv` 短链或 QQ 小程序卡片重定向到直播间/专栏/动态后，解析器只接受视频编号，导致报错“没有从链接中找到有效的 BV/AV/AU 号”，甚至可能把直播页推荐位的视频误当成分享目标。现在短链会先解析出真实内容类型再分流。
- 修复 `live.bilibili.com/h5/...`、`live.bilibili.com/blanc/...` 直播链接无法识别的问题。
- 修复 `bilibili://live/...`、`bilibili://opus/...`、`bilibili://article/...` 小程序 URI 无法识别的问题。
- 直播卡片新增主播头像（此前直播卡片的头像位置永远是占位图）。
- 修复开启语音转写后，即使视频已有公开字幕也会下载完整视频转写的性能问题；现在转写仅在无字幕时作为回退。
- 语音转写不再强制按中文识别，改为自动检测语言，外语视频不会再被硬转成乱码。

## v1.1.0

- 新增 B 站专栏（CV）解析：支持 `bilibili.com/read/cv...` 链接，展示标题、作者、分类、字数、摘要与统计。
- 新增 B 站直播解析：支持 `live.bilibili.com/...` 链接，展示标题、主播、分区、在线人数与直播状态。
- 新增 B 站动态解析：支持 `bilibili.com/opus/...`、`t.bilibili.com/...` 链接，展示作者、内容、图片与点赞/评论/转发统计。
- 新增通用内容卡片模板（`templates/content_card.html`），统一渲染专栏/直播/动态。
- 插件名称更新为「B站全内容解析」。

## v1.0.1

- 修复 B 站音频解析接口错误：改用正确的 `www.bilibili.com/audio/music-service-c/web/song/info` 接口，并修正 UP 主 UID 字段读取（`uid` 而非 `mid`），音频解析现在可以正常工作。
- 短链解析支持识别音频（AU）链接。

## v1.0.0

- 首个版本。自动识别并解析 B 站视频/音频链接、`BV`/`AV` 号、`b23.tv` 短链和 QQ 小程序分享。
- 视频解析支持长图卡片：封面、UP 主、统计、热门评论与源链接。
- 音频解析支持 `bilibili.com/audio/au...` 链接的长图卡片与文本模式。
- 可选使用 AstrBot 已配置的模型商生成视频 AI 总结（基于公开字幕或登录后 AI 字幕）。
- 可选视频下载（基于妖狐直链），带编号有效期、权限、冷却、每日上限、并发与体积/时长限制。
- 可选 `faster-whisper` 语音转写总结（需手动安装 ffmpeg 与 faster-whisper）。
