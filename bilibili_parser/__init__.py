"""Core helpers for the Bilibili parser plugin.

The package initializer intentionally has no eager imports, so parsing and
duplicate-guard tests can run before optional runtime dependencies are installed.
"""
